# Core dependencies - upgraded for compatibility
pip>=23.0
setuptools>=68.0
wheel>=0.41.0

# Web Framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Data Validation
pydantic>=2.12.4,<2.13.0
pydantic-settings>=2.12.0,<2.14.0

# Database
sqlalchemy==2.0.49

# Image Processing
pillow>=10.4.0
numpy>=1.26.0,<2.0  # Use NumPy 1.x for binary compatibility with OpenCV/PyTorch
pi-heif>=1.3.0

# Configuration
python-dotenv==1.0.0

# ML Framework (install without build from binary wheels)
torch==2.6.0 --only-binary :all:
torchvision==0.21.0 --only-binary :all:

# Computer Vision (use pre-built wheels)
opencv-python==4.8.1.78

# Damaged sign detection dependencies
ultralytics>=8.0.0
sentence-transformers>=2.3.0
faiss-cpu>=1.7.4
